/**
 * Default used gas limit for redeem transactions.
 */
export const DefaultRedeemGas: bigint = BigInt(72000);

/**
 * Minimum priority fee for EIP-1559 transactions (2 gwei).
 */
export const MinPriorityFee: bigint = BigInt(2e9);
